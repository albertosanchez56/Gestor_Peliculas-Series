spring:
  application:
    name: gateway-service3
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    gateway:
      enabled: true 