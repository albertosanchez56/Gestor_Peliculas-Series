server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/albertosanchez56/Gestor_Peliculas-Series.git
          search-paths: config-data
  application:
    name: config-server