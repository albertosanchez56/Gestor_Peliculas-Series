# Copia este archivo a .env antes de ejecutar docker-compose up
# No subas .env al repositorio.

# Contraseña del usuario root de MySQL (obligatoria en Docker)
MYSQL_ROOT_PASSWORD=tu-password-seguro

# JWT para auth (user-service y review-service)
JWT_SECRET=tu-clave-secreta-muy-larga-minimo-32-chars

# Clave interna entre microservicios (Gateway → Movie/Review/User)
INTERNAL_API_KEY=clave-interna-entre-microservicios

# TMDB: solo necesaria si usas importación de películas desde The Movie DB
TMDB_API_KEY=tu-api-key-de-themoviedb
