server:
  port: 0

spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  datasource:
    url: jdbc:mysql://localhost:3306/pruebagestorusuarios  # Cambia a la URL de tu base de datos
    username: root                               # Cambia por el usuario de tu base de datos
    password:                             # Cambia por la contraseña de tu base de datos
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Cambia a 'validate', 'none', o 'create' según tus necesidades
    show-sql: true      # Opción para mostrar las consultas SQL en la consola

security:
  jwt:
    secret: ${JWT_SECRET:vW9mF7kQ3zH1pA6nR2sX8cJ5yT4uE0iL9oB3gN7dP1rK6mQ2xS8aV5hZ0tY9uC}
    expiration-minutes: ${JWT_EXP_MINUTES:120}

